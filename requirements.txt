# Python dependencies
pypandoc>=1.4.0
scancode-toolkit==32.3.3

# Note: System-level dependencies must be installed separately. Ensure the following are available:
# - Pandoc (the converter binary)
# - A TeX engine with LuaLaTeX (e.g., texlive or basictex with luatex package)
# - Noto Color Emoji font and TeX Gyre Termes (or another mainfont) installed on the system
